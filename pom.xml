<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">


	<modelVersion>4.0.0</modelVersion>
	<groupId>com.biit</groupId>
	<artifactId>webforms</artifactId>
	<packaging>pom</packaging>
	<version>2.5.2</version>
	<name>Pilot Agile Testing for WebForms</name>
	<description>Form Designer</description>

	<modules>
		<module>webforms-logger</module>
		<module>webforms-persistence</module>
		<module>webforms-core</module>
		<module>webforms-gui</module>
		<module>webforms-integration-test</module>
		<module>webforms-release</module>
	</modules>

	<distributionManagement>
		<repository>
			<id>central</id>
			<name>biit-releases</name>
			<url>https://dev.biit-solutions.com:8443/mavenartifactory/ext-release-local</url>
		</repository>
		<snapshotRepository>
			<id>snapshots</id>
			<name>libs-snapshot</name>
			<url>https://dev.biit-solutions.com:8443/mavenartifactory/libs-snapshot</url>
		</snapshotRepository>
	</distributionManagement>

	<scm>
		<connection>scm:git:dev.biit-solutions.com:WebForms.git</connection>
		<developerConnection>scm:git:dev.biit-solutions.com:WebForms.git</developerConnection>
		<tag>webforms-2.5.2</tag>
	</scm>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<jetty-maven-plugin.version>9.2.7.v20150116</jetty-maven-plugin.version>
		<maven-compiler-plugin.version>3.1</maven-compiler-plugin.version>
		<maven-resources-plugin.version>2.7</maven-resources-plugin.version>
		<maven-antrun-plugin.version>1.7</maven-antrun-plugin.version>
		<maven-surefire.version>2.17</maven-surefire.version>
		<exec-maven-plugin.version>1.3.2</exec-maven-plugin.version>
		<org.testng.version>6.8.8</org.testng.version>
		<vaadin.version>7.3.8</vaadin.version>
		<vaadin.plugin.version>${vaadin.version}</vaadin.plugin.version>
		<mysql-odbc.version>5.1.31</mysql-odbc.version>
		<org.springframework.version>4.0.6.RELEASE</org.springframework.version>
		<aspectj.version>1.8.1</aspectj.version>
		<log4j.version>1.2.17</log4j.version>
		<slf4j-api.version>1.7.7</slf4j-api.version>
		<slf4j-log4j12.version>1.7.7</slf4j-log4j12.version>
		<hibernate.version>4.3.6.Final</hibernate.version>
		<liferay-service-common.version>0.4.6</liferay-service-common.version>
		<liferay-authentication-client.version>0.5.8</liferay-authentication-client.version>
		<form-structure.version>0.2.1</form-structure.version>
		<root.directory>${project.basedir}</root.directory>
		<itext.version>2.1.7</itext.version>
		<h2.version>1.4.181</h2.version>
		<ehcache.version>2.6.9</ehcache.version>
		<c3p0.version>0.9.2.1</c3p0.version>
		<DES-encryption.version>1.0.0</DES-encryption.version>
		<abcd.version>1.1.5</abcd.version>
		<jbool_expressions.version>1.6</jbool_expressions.version>
		<gson.version>2.3.1</gson.version>
		<stepper.version>2.2.0</stepper.version>
		<maven-failsafe-plugin.version>2.18.1</maven-failsafe-plugin.version>
		<vaadin-testbench.version>4.0.1</vaadin-testbench.version>
		<biit-logger.version>0.2.0</biit-logger.version>
		<maven-site-plugin.version>3.4</maven-site-plugin.version>
		<findbugs-maven-plugin.version>3.0.0</findbugs-maven-plugin.version>
		<mybatis.version>3.2.8</mybatis.version>
		<maven-release-plugin.version>2.5.1</maven-release-plugin.version>
		<maven-deploy-plugin.version>2.8.2</maven-deploy-plugin.version>
		<maven-scm-provider-gitexe.version>1.9.2</maven-scm-provider-gitexe.version>
		<project.scm.id>git</project.scm.id>
		<versions-maven-plugin.version>2.1</versions-maven-plugin.version>
	</properties>

	<dependencies>
		<!-- Testing -->
		<dependency>
			<groupId>org.testng</groupId>
			<artifactId>testng</artifactId>
			<version>${org.testng.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-site-plugin</artifactId>
				<version>${maven-site-plugin.version}</version>
				<configuration>
					<reportPlugins>
						<plugin>
							<plugin>
								<groupId>org.codehaus.mojo</groupId>
								<artifactId>findbugs-maven-plugin</artifactId>
								<version>${findbugs-maven-plugin.version}</version>
								<configuration>
									<effort>Max</effort>
									<xmlOutput>true</xmlOutput>
								</configuration>
							</plugin>
						</plugin>
					</reportPlugins>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven-compiler-plugin.version}</version>
				<configuration>
					<source>7</source>
					<target>7</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>${maven-resources-plugin.version}</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<version>${maven-release-plugin.version}</version>
				<dependencies>
					<dependency>
						<groupId>org.apache.maven.scm</groupId>
						<artifactId>maven-scm-provider-gitexe</artifactId>
						<version>${maven-scm-provider-gitexe.version}</version>
					</dependency>
				</dependencies>
				<configuration>
					<checkModificationExcludes>
						<checkModificationExclude>pom.xml</checkModificationExclude>
					</checkModificationExcludes>
					<preparationGoals>clean install</preparationGoals>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>